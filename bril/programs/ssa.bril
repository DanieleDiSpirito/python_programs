@main {
.entry:	
	a: int = const 47;
	zero: int = const 0;
	cond: bool = gt a zero;
	br cond .left .right;
.left:
	a: int = add a a;
	a: int = add a zero;
	jmp .exit;
.right:
	a: int = mul a a;
	jmp .exit;
.exit:
	print a;
}
