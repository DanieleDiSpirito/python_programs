@main {
.entry:	
	zero: int = const 0;
	one: int = const 1;
	cond: bool = gt one zero; # 1 > 0
	br cond .true .false;
.true:
	x1: int = add zero one;
	jmp .end;
.false:
	x2: int = add zero one;
	jmp .end;
.end:
	x3: int = phi .true x1 .false x2;
	print x3;
}
